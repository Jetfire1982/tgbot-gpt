# Установка базового образа
FROM node:16-alpine

# Установка директории приложения в контейнере
WORKDIR /app

# Копирование зависимостей и файла package.json в контейнер
COPY package*.json ./

# Установка зависимостей
RUN npm ci

# Копирование остальных файлов приложения в контейнер
COPY . .

ENV PORT 3000

EXPOSE $PORT

# Определение команды, которая будет запускаться при старте контейнера
CMD ["npm", "start"]